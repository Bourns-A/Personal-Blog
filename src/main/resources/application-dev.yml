spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:Mysql://localhost:3306/myBlog?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    username: 
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    com.bourns.blog: debug
  file:
    path: log/blog-dev.log

server:
  port: 8080